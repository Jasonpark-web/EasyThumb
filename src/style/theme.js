const size = {
  mobile: "767px",
  desktop: "768px",
};

const theme = {
  mainColor: "#FCE44D",
  textColor: "#374151",
  bgColor: "#fff",
  mobile: `(max-width: ${size.mobile})`,
  desktop: `(min-width: ${size.desktop})`,
};

export default theme;
